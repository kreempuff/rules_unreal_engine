# Design

All files meant to be run in a terminal are in the `bin` directory. All files meant to be imported are in the 
`lib` directory. All files meant to be run in a terminal are executable. All files meant to be imported are not 
executable.
